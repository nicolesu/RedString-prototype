import React from 'react';
import ConnectionList from '../components/ConnectionList';

const Connection = () => (
  <div>
    <h1 style ={{marginLeft: '2rem' }}>My Connections</h1>
    <ConnectionList/>
  </div>
)

export default Connection;