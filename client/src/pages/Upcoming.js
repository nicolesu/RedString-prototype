import React from 'react'
import UpcomingEventList from '../components/UpcomingEventList';

const Upcoming = () => (
  <div>
    <h1 style ={{marginLeft: '2rem' }}>UPCOMING EVENTS:</h1>
    <UpcomingEventList/>
  </div>
)

export default Upcoming;